export default {
  namespace: 'mainLayout',
  state: {
    iconCollapsed: false,
  },
  reducers: {
    changeIconCollapsed(state, { payload }) {
      // return {...state,iconCollapsed}
      //const reulst = Object.assign({ iconCollapsed });
      return { ...state, iconCollapsed: payload };
    },
  },
  effects: {},
  subscriptions: {},
};
